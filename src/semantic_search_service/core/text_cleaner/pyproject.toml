[build-system]
requires = ["scikit-build-core>=0.10.0", "pybind11>=2.13"]
build-backend = "scikit_build_core.build"

[project]
name = "text-cleaner"
version = "0.1.0"
description = "Fast C++ text cleaner for semantic search"

[tool.scikit-build]
cmake.version = "3.15"          # ✅ а не minimum-version